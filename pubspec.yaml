name: mojood_app
description: "Mojood App – Booking, Flights, Restaurants, Cars, Events, Taxi"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # ✅ Internationalization (مثبتة من flutter_localizations)
  intl: ^0.20.2

  # iOS Icons
  cupertino_icons: ^1.0.8

  # Supabase (Database + Auth)
  supabase_flutter: ^2.6.0

  # Share
  share_plus: ^10.0.0

  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # HTTP Requests
  dio: ^5.3.3
  http: ^1.1.0  # 🆕 مهم لـ Amadeus API

  # JSON Models
  json_annotation: ^4.9.0

  # UI / Fonts / Tools
  google_fonts: ^6.2.1
  smooth_page_indicator: ^1.1.0
  sizer: ^2.0.15

  # Local Storage
  shared_preferences: ^2.3.2

  # Navigation Router
  go_router: ^14.2.1

  # Google Maps
  google_maps_flutter: ^2.6.0

  # 🆕 URL Launcher (لفتح روابط الحجز الخارجية)
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Code generation for JSON models
  build_runner: ^2.4.9
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/logo/